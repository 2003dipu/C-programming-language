#include <stdio.h>
#include <string.h>

int main()
{
    char string1[] = "Rosmi";
    char string2[] = "Sinha";

    // strlwr(string1); // converts a string to lowercase
    // strupr(string2); // converts a string to uppercase
    // strcat(string1,string2); // appends or concatenates string2 to end of string1
    // strncat(string1,string2, 2); // appends or concatenates n characters from string2 to end of string1
    // strcpy(string1,string2); // copy string2 to string1
    //strncpy(string1, string2, 2); // copy n characters from  string2 to string1
    //strset(string1, '?'); // sets all characters of a string to a given character
    //strnset(string1, '?', 1); // sets all characters of a string to a given character
    //strrev(string1); // reverse a string
    //int length = strlen(string1); // calculate the length of a string
    //int result = strcmp(string1, string2); // string compare all characters. If strings are the same return 0, else -1;
    //int result = strnicmp(string1, string2, 1); // string compare all characters. If strings are the same return 0, else -1;
    /*
    if (result == 0)
    {
        printf("These strings are the same. result = %d\n", result);
    }
    else
    {
        printf("These strings are not the same. result = %d\n", result);
    }
    */


    //printf("%s", string1);
    // printf("%s", string2);
    //printf("Length of %s is %d", string1, length);

    return 0;
}