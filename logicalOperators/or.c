#include <stdio.h>
#include <stdbool.h>

int main()
{
    float temp = 26.78;
    int num1 = 3;
    int num2 = 12;

    if (temp <= 0 || temp >= 30)
    {
        printf("The weather is bad\n");
    }
    else
    {
        printf("The weather is bad\n");
    }

    if (num2 % num1 == 1 || num2 % num1 == 1)
    {
        printf(" Division %d / %d has remainder of 1\n\n", num2, num1);
    }
    else
    {
        printf(" Division %d / %d has no remainder\n\n", num2, num1);
    }

    return 0;
}